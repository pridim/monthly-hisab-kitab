import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.monthly.hisab.kitab.com',
  appName: 'monthly-hisab-kitab',
  webDir: 'build'
};

export default config;
